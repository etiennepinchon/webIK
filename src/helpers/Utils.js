export default class Utils {




  // static rotToQua(rot) {
  //   return Quaternion.euler(rot.x, rot.y, rot.z);
  // }
  //
  // static quaToRot(qua) {
  //   const degToRad = (Math.PI / 180.0);
  //   return Quaternion.toEulerAngles(qua).divideScalar(degToRad);
  // }

  //window._scene.updateMatrixWorld();

  // rotation.setFromQuaternion
}
